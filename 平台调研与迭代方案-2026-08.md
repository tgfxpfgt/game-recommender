# 平台调研与迭代升级方案（2026-08）

> 依据 Chrome 官方文档（developer.chrome.com）与官方博客的调研结论，结合本项目现状
> （v10.3.1，MV3，零构建）给出的可落地迭代方案。调研方法：官方 What's new / API
> 参考 / SW 生命周期文档 + 官方博客。

## 一、平台现状与本项目的符合度

| 官方方向 | 现状 | 结论 |
|---|---|---|
| MV3 为唯一标准；MV2 扩展 **2026-08-31** 全部从 CWS 移除 | 本项目自 v1.x 即 MV3 | ✅ 无迁移负担；上架窗口有利 |
| SW 状态持久化官方推荐：`chrome.storage.session`（会话级，1MB 默认配额）+ 启动恢复 | v10.0.0 已实现（诊断数据/任务检查点/通知内容） | ✅ 已对齐官方最佳实践 |
| SW 生命周期新行为：接收事件即保活（Chrome 官方博客 longer-esw-lifetimes）| v10.0.0 起任务可恢复化（session 检查点 + alarm 续跑），不再依赖 keepalive hack | ✅ 官方推荐的事件驱动 + 断点续跑模式 |
| `browser` 命名空间（Chrome 148）跨浏览器 API | 未采用 | ➖ "跨浏览器"为明确不做项，暂无动作 |
| `chrome.userScripts`（Chrome 120+，需用户开开发者模式） | 自定义站点注入用 `registerContentScripts`（数据驱动规则，无任意代码） | ➖ 无迁移必要；若未来做"用户自定义脚本"类功能再评估 |
| Dashboard 团队角色（admin/editor/item manager/viewer） | 未上架 | ℹ️ 上架后多人协作可用 |

## 二、可落地的迭代方案（按优先级）

### P1（低成本、直接收益）

1. **CWS 上架窗口**：MV2 大限（2026-08-31）后 CWS 竞争格局变化，MV3 且权限收敛的
   扩展过审预期良好。前置材料已就绪（STORE.md 清单），仅差开发者账号与素材定稿。
2. **storage.session 配额守卫**：`storage.session` 默认 1MB（Chrome 112+ 可
   `setAccessLevel`/配额查询）。当前用量 KB 级；建议在批量任务检查点写入处加
   字节数上限守卫（超限裁剪 imageData），防极端大列表（数百项 × 封面 URL）触顶。
   预估：~30 行。
3. **SW 启动路径瘦身**：官方推荐 SW 顶层只注册监听器、重活延后。当前 SW 顶层已
   较克制（warmup 全部异步化）——可把 `refreshFreeGames(false)` 启动触发改为首次
   打开限免页/角标点击时惰性触发，减少冷启动竞争。预估：~20 行。

### P2（中期、体验升级）

4. **Side Panel API**（Chrome 114+，稳定）：把 popup 的快捷设置/限免列表做成
   侧边栏常驻面板——浏览下载站时无需开浮窗。与现有 hub（iframe 中心）互补。
   预估：新 surface + 复用 popup 逻辑，1 个小版本。
5. **Offscreen Document**（Chrome 109+）：若未来需要 DOM 能力（如二维码从
   offscreen 解码、音频提示），用 offscreen document 而非内容脚本承担。
   当前二维码在内容侧解码已够用——记录待用。
6. **declarativeNetRequest**：当前无广告拦截/请求改写需求；若未来做"站内
   规则净化"（去广告/去推荐位），DNR 是 MV3 唯一路径。记录待用。

### P3（远期/观望）

7. `browser` 命名空间（Chrome 148+）：跨浏览器前缀——若未来重启跨浏览器目标
   （Firefox AMO），迁移成本低（本项目 API 面窄：storage/alarms/tabs/scripting）。
8. **userScripts API**：仅当引入"用户编写注入脚本"功能时启用（需开发者模式开关，
   CWS 审核需说明用途）。

## 三、明确不建议

- keepalive hack 库（force-alive 类）：官方已改进生命周期 + 本项目已断点续跑化，
  hack 反而增加被 CWS 审核质疑的面。
- 迁移 TypeScript / 打包器：零构建是项目核心约定（见 AGENTS.md）。

## 参考来源

- What's new in Chrome extensions: https://developer.chrome.com/docs/extensions/whats-new
- SW 生命周期: https://developer.chrome.com/docs/extensions/develop/concepts/service-workers/lifecycle
- Longer extension SW lifetimes（官方博客）: https://developer.chrome.com/blog/longer-esw-lifetimes
- chrome.storage: https://developer.chrome.com/docs/extensions/reference/api/storage
- chrome.userScripts: https://developer.chrome.com/docs/extensions/reference/api/userScripts
- MV2 退役时间线: https://developer.chrome.com/docs/extensions/develop/migrate/mv2-deprecation-timeline
