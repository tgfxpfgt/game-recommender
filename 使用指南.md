# Game Recommender 使用指南（中文）

> 适用版本：v3.3.7 · 基于浏览行为学习的游戏资源智能推荐 Chrome 扩展
> 本指南面向普通用户，介绍安装、配置与日常操作。技术细节见《项目介绍与评估报告》。

---

## 一、安装步骤（开发者模式加载）

1. 把本项目代码下载 / 克隆到本地，确保文件夹里包含 `manifest.json`。
2. 打开 Chrome 浏览器，在地址栏输入 `chrome://extensions/` 并回车。
3. 打开页面右上角的「**开发者模式**」（Developer mode）开关。
4. 点击「**加载已解压的扩展程序**」（Load unpacked），选择项目的**根目录**文件夹。
5. 建议把扩展图标固定到工具栏。点击图标打开弹窗，看到版本号 `v3.3.7` 即表示加载成功。

> 💡 若弹窗提示「扩展后台未就绪」，属 Manifest V3 Service Worker 冷启动的正常现象，稍等几秒或刷新页面即可。

---

## 二、配置方法

点击扩展图标 → 「⚙ 设置」（或右键图标 → 选项），打开设置页。

设置页采用**左侧分类 + 右侧面板**布局，**修改后约 800ms 自动保存**，无需手动点击保存（也可点底部「立即保存」）。

### 常用配置项一览

| 配置项 | 作用 | 推荐设置 |
|--------|------|----------|
| 启用插件 / 工作状态浮窗 | 总开关与状态栏显隐 | 默认开启 |
| 推荐高亮阈值（30%–90%） | 列表页高于此推荐值的游戏高亮 | 60% 起 |
| Steam 好评率过滤 | 开关 + 0%–95% 阈值（步进 5%） | 只看好评游戏可设 70%+ |
| 虚拟机版过滤 | 隐藏「虚拟机板」类游戏，关键词可改 | 按需开启 |
| 推荐算法权重 | 点击率 / 下载率 / 关键词 / Steam 评分 | 保持权重总和，调高 Steam 权重更看口碑 |
| AI 大模型推荐 | Ollama 本地 / OpenAI 兼容，填端点与模型 | 进阶用户；填错自动回退内置算法 |
| 缓存有效期 | 各模块 TTL（小时/天/月/年，0=长期） | 默认即可，慢网可放宽 |
| 追踪网站管理 / Steam 检索范围 | 勾选要追踪的站点与检索的下载站 | 按常逛站点勾选 |
| 适配规则管理 | 编辑 / 导入导出 / 恢复内置规则 | 一般无需改动 |
| 日志 / 备份 | 级别、保留天数、自动备份间隔 | 建议开启自动备份防数据丢失 |

---

## 三、日常操作方式

### 🟢 在下载站列表页（如 XDGame / 咸鱼单机 / Gamer520 / 3DM / 游侠 / 游民星空）
- 每个游戏标题前自动出现**三段式徽章**：近 30 天好评率 → 全部好评率（分级着色，可点击跳转 Steam）→ 最近更新日期。
- 🎯 徽章显示推荐概率，高分项会被高亮。
- 右下角状态浮窗显示「查询 / 命中 / 过滤 / 未找到」统计。
- 点击徽章即可跳转 Steam 游戏详情页。
- 0 评测游戏显示灰色 AppID；未匹配显示「未找到」。

### 🔵 在下载站详情页
- 右上角浮窗展示 Steam 评价、中文支持情况、用户标签、发行日、最近更新、简体中文评测摘要。
- 左下角显示**下载历史**（上次下载时间、站点、次数，可快捷跳转）。
- 若搜索无匹配，可手动选择候选游戏并持久化映射。

### 🟠 在 Steam 游戏页面
- 右上角浮窗自动检索 XDGame / 咸鱼单机 / Gamer520 的对应资源，显示更新日期、版本、大小、网盘链接（自动拼接提取码）。
- 可直接跳转下载站详情页获取资源。

### 🟣 弹窗与限免
- **弹窗**：开关插件、调阈值、看统计、看 Steam API 状态、🔄 刷新、♻️ 强制刷新（清除当前页缓存后重载）。
- **限免页**：每日自动刷新 Epic / GOG / GamerPower 限免游戏，工具栏角标显示未领取数量，支持一键领取跳转。
- **数据分析页**：行为统计、关键词偏好、游戏缓存管理（多条件检索、分页、删除、手动更新、清理过期、修复异常名）。

---

## 四、常见问题（FAQ）

**Q：某游戏长期显示「未找到」或灰色 AppID？**
A：先用弹窗的「♻️ 强制刷新」清除缓存并重载当前页；插件会在忽略缓存有效期的情况下重新从 Steam 获取。

**Q：列表页大量游戏没有好评率？**
A：看弹窗「📡 Steam API 状态」是否变红（限流）。插件已自动降速，稍后重试即可；也可先翻到其他页再回来，利用下一页预载机制秒开。

**Q：如何新增一个下载站？**
A：高级用户可在「设置 → 适配规则管理」中编辑规则（JSON），无需改代码；保存后覆盖内置规则，可导出分享。

**Q：数据会丢吗？**
A：建议在设置中开启「自动备份」。所有数据存于 OPFS（不可用时降级 storage.local），支持手动备份/恢复与模块勾选导入导出。

**Q：显示异常或想彻底重置？**
A：设置页可「恢复默认设置」；数据分析页可清空游戏缓存；需要完全重来可在 `chrome://extensions/` 移除后重新加载。

---

## 五、权限说明

| 权限 | 用途 |
|------|------|
| `storage` | 存储用户偏好、浏览记录、缓存数据 |
| `alarms` | 定时刷新限免游戏与自动备份 |
| `<all_urls>` | 内容脚本注入与跨域请求（下载站 / Steam / 限免 API / 用户自定义 LLM 端点） |

所有外部请求均经过 host 校验（仅 http/https，拒绝私有/环回地址）以防 SSRF；动态内容均经 HTML 转义防 XSS。

---

*本指南与《项目介绍与评估报告》配套使用。扩展仅供学习和个人使用。*
