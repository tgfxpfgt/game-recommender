# 优化分析与建议（2026-09）

> 基于 v10.5.4（752 test / E2E 46 / gate 全绿）的全量代码分析。每条建议标注
> 「现状证据 → 建议 → 收益/工作量」，按用户指定七个方向组织，末尾给优先级路线。
> 分析方法：端点清单枚举（12 个出站域）、存储模块清单（21 个 OPFS 模块）、
> 批次/速度常量核对、UI 结构走查；未做用户端埋点测量——性能数字建议落地前实测。

---

## 方向一：增强缓存机制

现状：21 个 OPFS 存储模块；steam-cache 四模块（meta 30d / rating 7d / detail 72h / spy 7d）；
负缓存 2h；search-cache 24h LRU200；llm-cache 7d；app-stats 永久；steam250Rank 24h 快照。

### C1（P0）steam-cache 全量序列化 → 按模块分文件
- 证据：steam-cache 每次落盘 `JSON.stringify` 整个 500+ 条目 × 四模块数据（v10.4.3 实测
  满载单批 ~1MB，写放大已量化）；批量好评率改为每批落盘后写放大 ×5。
- 建议：steam-cache 拆为四个模块文件（meta/rating/detail/spy 各一），flush 只写脏模块；
  迁移：首读旧文件拆分写入，旧文件保留一个版本后删除。
- 收益：写量降 ~60-75%；单批落盘延迟等比下降。工作量：中（触及 steam-cache.js 全部
  读写面 + 迁移），建议单独立项。

### C2（P1）封面图 Cache API 缓存（cache-first）
- 证据：封面/头图全部直连 Steam CDN（cdn.akamai.steamstatic.com），列表页二次滚动
  每张图都走网络；无任何本地图片缓存。
- 建议：SW（MV3 无 SW 层缓存可用——用 `caches.open('gr-covers')` 在 content 不可行，
  需经后台 `fetch → cache.put`；或更简单：OffscreenDocument/imageBlob → objectURL 仅
  会话级）。推荐方案：后台新增图片缓存通道（复用 FETCH_IMAGE_DATA_URL 的取图通量，
  LRU 上限 200 张/5MB），列表页封面 `src` 换 dataURL 命中时零网络。
- 收益：列表页二次滚动封面秒出；弱网/Steam 被墙时封面仍可显示（配合 CDN 备选域）。
  工作量：中。

### C3（P1）HTML 商店页与新闻接口缺缓存
- 证据：`fetchStorePageHtml`（整页 HTML，单次数十至数百 KB）与 `fetchLastUpdate`
  （GetNewsForApp）在每次**详情全量获取**时都发起，无任何缓存（api-assemble.js:133/151）。
- 建议：store page 解析结果（chineseSupported/lastUpdate/tags 已在 detail 模块的）与
  news lastUpdate 各自加 TTL 缓存（72h / 7d，更新频率低）；store page 仅在 detail
  模块缺失/过期时拉取。
- 收益：详情全量获取网络请求 -2 次/游戏。工作量：小。

### C4（P2）缓存"省流量"量化
- 证据：分模块命中率已有（getCacheStats），但只有 hits/misses，没有换算成"省下的
  请求数/字节数"。
- 建议：命中时按端点累计 savings 计数（appdetails≈8KB、appreviews≈15KB、
  storesearch≈10KB），dashboard 显示"本次会话已省 N 次请求 / ~N MB"。
- 收益：缓存价值可见，也为后续调 TTL 提供数据。工作量：小。

### C5（P2）数据管理页加模块体积列
- 证据：数据管理列出各模块条目数（count），无字节大小；steam-cache 满载可达数 MB，
  用户无感知。
- 建议：GET_DATA_MODULES 增加 `bytes`（JSON.stringify 长度），列表加体积列 + 总量。
- 收益：存储健康可视化的最后一块。工作量：很小。

---

## 方向二：减少网络访问

现状：12 个出站域；单游戏全量获取 = appdetails(schinese+english) + appreviews +
steamspy + store page + GetNewsForApp ≈ 5-6 次请求；列表页未知游戏 ≈ storesearch(1-3
次变体) + appdetails + appreviews。

### N1（P0）english appdetails 按需化
- 证据：英文名（englishName）用于注册表/名称自愈，但列表页徽章只显示中文名。
  `fetchSteamAppDetails(appId, 'english')` 与 schinese 各发一次——每次详情 ×2。
- 建议：english 仅在 schinese 响应缺英文名字段时补发（appdetails schinese 已含
  name_en 场景有限，但 name/steam_appid 足够徽章用）；或 english 结果入 meta 模块
  缓存（30d）——当前 detail 模块有缓存，确认 english 是否复用 detail 缓存（若复用
  则仅首刷 ×2，收益有限——需实测确认后再动）。
- 收益：首刷详情请求 -50%。工作量：小（先加日志确认实际调用频率）。

### N2（P1）ISteamNews / store page 结果并入 detail 模块（同 C3）
- 收益：详情获取从 5-6 次降到 3-4 次。工作量：小-中。

### N3（P1）列表页 spy 模块延迟化
- 证据：列表页轻量好评率路径不需要 spy（时长/热度仅推荐引擎用）；确认
  getSteamPositiveRate 不拉 spy ✓（已轻量化）。剩余优化：spy 改为**详情浮窗打开时**
  拉取（当前详情全量获取时拉），列表场景永不触发。
- 收益：详情获取 -1 请求（spy 无官方替代、慢且偶发人机验证）。工作量：小。

### N4（P2）免费源聚合条件化
- 证据：refreshFreeGames 启动即拉 4-5 个限免源（Epic/GOG/GamerPower/Steam）；
  用户不开限免功能时全量浪费。
- 建议：`freeGamesEnabled` 开关（默认开）——关闭时 alarm/启动均跳过（角标与限免页
  显示旧数据并提示）；与 v10.3.0 的开关体系一致。
- 收益：不用限免功能的用户每日 -5 次外部请求。工作量：小。

### N5（P2）Steam CDN 备选域
- 证据：封面固定 cdn.akamai.steamstatic.com；被墙/CDN 抖动时全灭。
- 建议：封面加载失败时按序换备用域（cdn.cloudflare.steamstatic.com /
  media.steampowered.com），纯前端 onerror 链，无新请求逻辑。
- 收益：弱网环境封面可用性。工作量：很小。

---

## 方向三：提高响应速度

现状：内容 boot 30-90ms ✓；SW 冷启动 warmup 并行 ✓；列表批次 6 并发（anomaly 2）、
每批 60 名、45s 强制收尾；每批落盘（v9.7.0 决策）。

### S1（P0）视口内优先调度
- 证据：首屏 60 个按入队顺序发，用户视口内前 10-20 个可能与队尾混排；中国网络
  单请求 5-15s 时首屏可见区长时间无徽章。
- 建议：fireBatch 前用 getBoundingClientRect 粗排——视口内（±一屏）的名字前置；
  滚动哨兵已有，仅排序改动。
- 收益：首屏可感知出徽章时间约减半（先出的都是用户正看着的）。工作量：小。

### S2（P1）SW 冷启动 settings 0ms 化
- 证据：v10.4.4 条件加载后 boot 第一跳 = GET_SETTINGS（一次消息 RTT + SW 冷启动
  叠加）。
- 建议：`chrome.storage.session` 常驻"上次 settings"副本（saveSettings 时写入），
  boot 先用副本、后台异步校准。
- 收益：内容 boot 首跳省一次跨世界消息 + 后台读盘。工作量：小。

### S3（P1）大列表分片渲染
- 证据：130 项列表 DOM 一次性插入；千项级站点会有可感知卡顿。
- 建议：徽章插入按批 requestIdleCallback 分片（空闲回调基建 v10.5.2 已引入）。
- 收益：大列表滚动不卡。工作量：小-中。

### S4（P2）写放大根治（同 C1）
- C1 落地后每批落盘从全量 ~1MB 降为脏模块 ~百 KB 级，批间落盘延迟显著下降。

---

## 方向四：美化界面

现状：20+ 主题、floats 统一管理、gr-* 组件体系、XDGame 同口径评价卡（v10.5.3）。

### U1（P1）徽章设计语言统一
- 证据：列表页现有 综合评分⭐ / 近30天 / 全部 / 更新 / a-b / 推荐值 六种徽章，
  分属五个迭代批次——padding、字号、圆角、配色各成体系，并列时观感杂。
- 建议：徽章 CSS 变量化（--gr-badge-h/--gr-badge-fs/--gr-badge-radius/
  --gr-badge-gap 一处定义），六种徽章统一规格；hover 明细已各自完善保留。
- 收益：视觉一致性立竿见影；新增徽章成本降为一行变量引用。工作量：小。

### U2（P1）徽章过多折叠
- 证据：全开时单游戏最多 6-7 个徽章，挤压标题。
- 建议：超过 3 个时折叠为「+N」hover 展开（CSS :hover 或点击弹出，纯 CSS 可做）。
- 收益：标题可读性。工作量：中（需处理 tooltip/点击跳转的交互保留）。

### U3（P2）浮窗质感升级
- 证据：gr-float 已主题化；视觉仍偏"平"。
- 建议：backdrop-filter: blur + 统一阴影层级（--gr-shadow-sm/lg），一次改动全部
  浮窗受益；低配设备可开关。
- 收益：现代感。工作量：很小。

### U4（P2）dashboard 图表交互化
- 证据：趋势图手绘 SVG 无 hover；数据点定位靠肉眼。
- 建议：SVG 上加透明命中区 + 轻量 tooltip（当前值/日期），不引库。
- 收益：数据可读性。工作量：小-中。

---

## 方向五：提升 UI 交互逻辑与效率

### I1（P0）合并列表页两个悬浮齿轮
- 证据：列表页现有两个 FAB——布局定制齿轮（gr-xdgrid-fab）与过滤控件
  （gr-rfilter-fab，v10.5.1），功能同属"本页浏览体验"，按钮重复占位。
- 建议：合并为一个 FAB + 面板双标签（「布局」「过滤」）；两模块保留，UI 层合并。
- 收益：右下角只留一个入口；设置聚合。工作量：小-中。

### I2（P1）浮窗状态记忆补全
- 证据：浮窗位置有记忆（grFloatPos），**折叠状态不记忆**——每次刷新都展开。
- 建议：折叠状态入 chrome.storage.local（grFloatFolded:<id>），创建时恢复。
- 收益：高频用户少点一次。工作量：很小。

### I3（P1）过滤实时调节全覆盖
- 证据：悬浮过滤控件仅覆盖 总好评率（enableRatingFilter + min）；
  30 天过滤/重排序仍在设置页。
- 建议：过滤面板加 30 天阈值滑块 + 重排序开关（复用 applyLiveRatingFilter，
  重排序触发 sortItemsByRating）。
- 收益：实时调节能力对齐设置页。工作量：小。

### I4（P2）缓存管理批量操作
- 证据：缓存管理仅单条删除/全清。
- 建议：多选（checkbox）+ 批量删除 + 按站点/按游戏名过滤批量清理。
- 收益：精细清理能力。工作量：中。

### I5（P2）a-b 徽章点击 = 行为统计面板
- 证据：a-b 徽章点击无动作（好评率徽章点击跳 Steam）。
- 建议：点击弹 mini 面板显示该游戏的本站/跨站计数明细与最近记录时间。
- 收益：数据可解释性。工作量：小-中。

---

## 方向六：新增功能建议

### F1（P1）ITAD 价格历史与最低价提示
- 证据：ITAD 通路已接（多套 key 配置 + 二次校验），只用了"确认免费"一个能力。
- 建议：详情浮窗加"历史最低价 / 当前是否历史低价"（ITAD prices/v1 端点，带缓存）；
  愿望单游戏降价推送（结合 F2）。
- 收益：对下载站用户（比价心理）价值高；复用现有 key 基建。工作量：中。

### F2（P1）本地收藏/关注清单
- 证据：dislike 负反馈已有（v6.3.2），无正向收藏。
- 建议：列表/详情一键收藏（徽章 ✩ 按钮），dashboard 愿望单页；结合限免监控——
  收藏游戏进限免时优先通知。
- 收益：粘性最强的轻功能；数据本地。工作量：中。

### F3（P2）跨缓存游戏搜索
- 证据：steam-cache/registry/name-index 里已有数百游戏，无统一搜索入口。
- 建议：设置页或 hub 加全局游戏搜索（名称模糊 → 命中缓存显示评分/排名/本站
  下载历史/a-b 统计）。
- 收益：把已有数据资产变成可检索能力。工作量：中。

### F4（P2）主题定时切换
- 证据：20+ 主题手动切。
- 建议：日间/夜间主题对 + 按时间段自动切换（hooks 已有 applyTheme 单点）。
- 工作量：很小。

### F5（P3）备份加密
- 证据：备份明文 JSON 且密钥清空导出（安全设计 ✓）。
- 建议：可选 passphrase 加密（WebCrypto AES-GCM），供用户云盘备份场景。
- 工作量：中。

---

## 方向七：已有功能优化

### E1（P1）负缓存游戏重试节奏
- 证据：负缓存 2h + 失败重试长冷却 1h；"未找到"游戏每次列表页仍占请求配额。
- 建议：notfound 徽章 hover 显示"下次重试时间"；设置页暴露负缓存 TTL 已有——
  补"立即重试本页"按钮（复用 FORCE_REFRESH_PAGE）。
- 工作量：小。

### E2（P1）名称自愈后台化验证
- 证据：v10.5.2 已"名称自愈后台化"——验证列表页首屏不再被自愈请求阻塞
  （content-sim 有覆盖；建议真浏览器实测一次首屏时间对比）。

### E3（P2）二维码 CSS 背景图站点不覆盖（已知限制）
- 证据：解码器扫描 img/canvas；background-image 形式的二维码（部分站）不覆盖。
- 建议：MutationObserver 扩展扫描 computedStyle backgroundImage 含 qr 特征的元素
  （成本高）或先记录为已知限制。工作量：中，收益视站点。

### E4（P2）CI visual advisory 恢复阻断
- 证据：v10.5.1 并行会话将视觉回归拆为非阻断作业；基线漂移（如 v10.4.4 prettier
  重排引起的 0.60%）会静默积累。
- 建议：保留 advisory 但加"连续 2 次失败转阻断"的规则，或基线更新必须人工确认。

### E5（P3）diagnostic 探针工具化
- 证据：本轮多次用一次性探针（真浏览器 + runtimeLog）定位问题。
- 建议：tests/e2e-probe.mjs 固化为参数化诊断工具（--mock --dump-logs --url），
  下次排障零搭建。

---

## 优先级路线（建议两个批次）

**批次 A（一个小版本，体验+省流立竿见影）**
S1 视口优先 + I1 合并双齿轮 + I2 折叠记忆 + U1 徽章统一 + N5 CDN 备选 +
C5 体积列 + E1 重试提示 —— 全部小工作量，一个批次可完成。

**批次 B（一个中版本，缓存架构升级）**
C1 steam-cache 分模块分文件（含迁移）+ C3 商店页/新闻缓存 + N3 spy 延迟化 +
S4（随 C1 天然解决）—— 触及核心缓存读写面，需独立回归。

**观察项（按数据决定）**：N1 english 按需化（先实测调用频率）、N4 限免开关、
F1 ITAD 价格历史、F2 收藏清单。

## 明确不建议

- 迁移打包器/框架（零构建是核心约定）
- 追加 keepalive hack 库（官方生命周期已改进 + 任务已断点续跑化）
- steam250 全站爬取（仅 top250 快照即可覆盖需求，尊重站点）

---

*分析方法：出站端点枚举（fetchWithTimeout 调用面）、21 存储模块清单、批次/速度
常量核对、浮窗与徽章结构走查；性能数字（首屏时间/写放大）引用 v9.7.0/v10.4.3
实测基线，落地前建议按 AGENTS.md 惯例加性能日志实测。*
