# Game Recommender 后续开发 Agent 提示词（续作任务书）

> 适用对象：在 **zcode** 平台运行、底层模型为 **deepseek v4 flash** 的开发 agent。
> 目标：基于现有代码与评估报告，系统性消除技术债、补齐功能缺口、提升工程质量，按分阶段计划推进本项目（Chrome MV3 扩展 Game Recommender v3.3.7）。
> 配套必读资料（位于项目根目录）：
> - `GameRecommender-项目介绍与评估报告.html`（完整代码审阅报告，含结构图/架构图/路线图）
> - `使用指南.md`（安装/配置/日常操作）
> - `README.md`（功能清单、权限表、变更日志）
> 项目根目录：`F:\data\browser extension\game-recommender`

---

## 0. 任务背景与现状速览

- 这是一个**完成度极高、综合评级 A 级**的 Chrome MV3 扩展：浏览行为学习推荐 + Steam 评分集成 + 多下载站检索 + 限免监控。
- 架构：后台为 `module` 类型 Service Worker，分层 `core → storage → 业务层 → handlers → 入口`（单向依赖、无循环）；内容脚本为经典脚本，经全局 `window.__GR__` 共享。
- 当前规模：约 60 个 JS 文件，228 项自动化测试 **全部通过**（`node tests/run-tests.js`）。
- 设计约束：**刻意零框架、零构建、零运行期 TypeScript**，浏览器原生直接加载。
- 主要优势（保持）：分层清晰、三层缓存成熟、MV3 工程实践扎实、安全到位（SSRF+XSS+规则注入防护）、OPFS 数据层先进、228 测试全绿。
- 主要短板（本任务重点治理）：无类型系统 + 内容脚本全局耦合 + Steam 页面正则解析脆弱，及若干较低优先级项。

**你的工作原则**：先理解后动手；任何改动不得破坏现有 228 项测试；改动必须可单测；保持「零框架零构建」约束（除非进入阶段一类型检查项，且仅编译期）；保持安全水位与单向依赖。

---

## 1. 项目硬约束（红线，违反即判定不合格）

1. **测试零回归**：每次改动后必须运行 `node tests/run-tests.js`，结果须为全绿（当前 30+35+39+110+14 = 228）。任何新增/修改逻辑必须补对应 `tests/*.mjs` 用例。
2. **零框架零构建约束**：除非进入「阶段一·引入类型/静态检查」项，否则**不得**引入打包器、UI 框架、运行期 TypeScript/类型依赖。类型检查仅限编译期（见 §2-D）。
3. **安全水位不降**：SSRF host 校验、动态内容 `escapeHtml/escapeAttr` 转义、规则导入纯数据白名单 + 拒绝函数注入，必须随改动同步保留并补测试。
4. **单向依赖不可破**：`core → storage → 业务层 → handlers → 入口`；内容脚本侧也不得制造循环引用。新增模块必须声明其依赖方向。
5. **manifest 权限最小化**：维持 `storage` + `alarms` + `host_permissions: <all_urls>`；不擅自增删权限，确需增加时须说明理由。
6. **变更可控**：用户偏好「功能完整 + 有测试 + 自动 git 提交」。每完成一个独立子任务，做一次 git 提交并写清根因（参照 README 变更日志风格）。

---

## 2. 第一部分：缺点 · 风险 · 隐患与整改建议（逐条带来源）

> 下表汇总报告 §6「不足与风险」与 §8「可维护性隐忧」的全部条目，已合并同一问题在两处的出处。
> 严重度：🟥 较高 / 🟧 中等 / 🟦 较低。来源列含「报告章节」与「实际代码位置」，agent 须先 `Read` 对应文件再动手。

| 编号 | 类别 | 严重度 | 问题描述 | 来源（报告 / 代码） | 整改建议 | 优先级 |
|------|------|--------|----------|---------------------|----------|--------|
| **A** | 风险+隐患 | 🟧 中等 | **双重范式 + 全局命名空间**：后台用 ES Module，内容脚本用 `window.__GR__` 全局对象挂模块（`GR.common/debug/builder/list/detail` 等均以 IIFE 挂全局）。隐式耦合，IDE 无法静态追踪依赖，重构极易遗漏。 | 报告 §6、§8；`content/tracker.js`、`content/core/floats.js`、`content/core/common.js` 等 | 将 `content/**` 经典脚本迁移为 **ES Module**，用显式 `import` 替代 `__GR__` 全局耦合（MV3 已支持以模块方式加载内容脚本）。迁移可分批、按模块推进，每批不影响线上功能。 | **P0** |
| **B** | 风险+隐患 | 🟧 中等 | **常量重复定义**：`noisePattern` 正则同时出现在 `title-parser.js` 与 `detail-page.js`；`escapeHtml/escapeAttr` 在 `shared/escape.js` 与 `content/core/common.js` 各有一份。改一处易漏另一处。 | 报告 §6、§8；`background/steam/title-parser.js:11`、`content/detail/detail-page.js:55`、`shared/escape.js`、`content/core/common.js:20` | 抽公共定义：`noisePattern` 并入 `shared/constants.js`；`escapeHtml/escapeAttr` 统一由 `shared/escape.js` 导出，后台与内容脚本均 import 复用，删除重复副本。 | **P0** |
| **C** | 风险 | 🟧 中等 | **Steam 页面解析依赖正则**：中文支持表、用户标签、详情页元信息均用正则抓取商店页 HTML。站点改版即失效，历史已多次因此修复。 | 报告 §6；`background/steam/api.js` 的 `parseChineseLanguageSupport/parseUserTags`，`background/sites/search.js` 的 `extractDetailMeta` | 优先用 Steam 官方 API 字段替代商店页正则；正则解析结果加容错与缓存，**失败时降级而非报错**（如缺中文支持信息时回退「未知」而非崩溃）。 | **P1** |
| **D** | 风险 | 🟥 较高 | **无类型系统**：纯 JS，约 60 文件数千行。历史上曾因 TDZ 导致整个后台加载失败（v1.11.1）、`steam_appid` 字段不兼容致详情页 100% 失败（v3.3.4），靠测试/人工审阅兜底（`core/constants.js` 用字符串字面量规避 TDZ 即为此留下痕迹）。 | 报告 §6、§8；`README.md` v1.11.1 / v3.3.4 变更记录；`background/core/constants.js` | 引入 **JSDoc 类型标注 + eslint**（或试水 TypeScript 仅编译期，运行期仍原生 JS），在 CI 拦截 TDZ/字段误用类问题。先对 `core/constants.js`、`steam/api.js`、`recommend/engine.js` 等关键纯函数模块补类型。 | **P0** |
| **E** | 隐患 | 🟧 中等 | **站点选择器硬编码**：各下载站的容器/标题选择器写死在规则文件，页面结构变化需手动更新规则。 | 报告 §6；`adapters/sites/*.js` 的 `listItem.containers` 等 | 借「数据驱动」优势，为选择器增加**容错匹配**（多候选选择器 + 结构推断回退），并将选择器与提取逻辑进一步与数据规则解耦，降低单点改版影响。 | **P1** |
| **F** | 隐患 | 🟦 较低 | **列表扫描上限 500 链接**：通用列表页判断至多扫描 500 个 `a` 标签，超大页面可能漏判或开销偏高。 | 报告 §6；`content/adapters/builder.js:79` | 改为**按需/可见区域触发**扫描（IntersectionObserver/分页），限制全页链接扫描开销。 | **P2** |
| **G** | 风险 | 🟦 较低 | **无浏览器端 E2E 测试**：测试仅覆盖纯函数/单元/接口模拟，缺乏真实页面注入与交互的端到端验证。 | 报告 §6、§8；`tests/` 均为 `*.mjs` 逻辑测试 | 阶段一补充轻量 E2E（Playwright 加载扩展到 Chrome 冒烟），或在 CI 增加「加载到 Chrome 冒烟」步骤。 | **P1** |
| **H** | 缺口 | 🟦 较低 | **无 i18n**：UI 文案与注释硬编码中文，国际化需整体改造。 | 报告 §6；全部前端文件 | 抽离 UI 文案为 i18n 资源（如 `shared/i18n/*.json`），先提供英文界面占位，后续填充。 | **P2** |

> **重点提醒**：A、B、D 三项（全局耦合、常量重复、无类型）是报告反复强调的「最大维护隐患」，投入小、回报高，必须作为每个新功能开发前的**固定前置**优先消除。

---

## 3. 第二部分：后续开发建议（技术债 + 功能缺口，优先级与改进方向）

> 优先级：🔴 P0 = 高（质量/稳定）/ 🟠 P1 = 中（体验/扩展）/ 🔵 P2 = 低（锦上添花）。
> 表中「技术债」对应 §2 编号，「功能缺口」为本任务书新增识别。

### 阶段一 · 质量加固（建议 1–2 周，P0 优先）

| 目标 | 内容 | 类型 | 优先级 | 预期改进方向 / 验收 |
|------|------|------|--------|---------------------|
| 消除重复常量 | 见 §2-B | 技术债 | 🔴 P0 | `noisePattern` 入 `shared/constants.js`；`escapeHtml/escapeAttr` 统一 `shared/escape.js` 导出并删除副本；跑全量测试 + 补「重复定义已消除」断言 |
| 内容脚本模块化 | 见 §2-A | 技术债 | 🔴 P0 | 分批将 `content/**` 迁移为 ES Module，显式 import 替代 `__GR__`；迁移后 popup/options/列表/详情页功能无回归 |
| 类型/静态检查 | 见 §2-D | 技术债 | 🔴 P0 | 接入 eslint + JSDoc；关键纯函数模块补类型；CI 增加 lint/类型检查步；至少拦截一类历史 TDZ 类问题 |
| CI 实测 | 见 §2-G | 技术债 | 🟠 P1 | 核实 `.github/workflows/ci.yml` 真实运行；补充 Chrome 加载冒烟或 Playwright 轻量 E2E |

### 阶段二 · 体验与覆盖（建议 2–4 周，P1）

| 目标 | 内容 | 类型 | 优先级 | 预期改进方向 / 验收 |
|------|------|------|--------|---------------------|
| 降低解析脆弱性 | 见 §2-C | 技术债 | 🟠 P1 | 优先用 Steam 官方 API 字段替代商店页正则；解析加容错+缓存，失败降级不报错；补「解析失败降级」测试 |
| 扩充下载站 | 新增 2–3 个站点 | 功能缺口 | 🟠 P1 | 仅增 `adapters/sites/*.js` 数据规则（零代码入侵）；支持**用户社区规则市场**（导入/导出/分享） |
| 推荐引擎增强 | 见下方功能缺口 G1–G3 | 功能缺口 | 🟠 P1 | 引入价格/时长/近期热度信号；UI 直观呈现推荐理由；LLM 评分输出可解释文本；补对应 engine 测试 |
| 列表性能优化 | 见 §2-F | 技术债 | 🔵 P2 | 预载下一页改为按需/可见区域触发；超大列表分页处理；扫描开销下降且功能无回归 |

### 阶段三 · 生态扩展（建议 1–2 月，P2）

| 目标 | 内容 | 类型 | 优先级 | 预期改进方向 / 验收 |
|------|------|------|--------|---------------------|
| 数据分析增强 | 见功能缺口 G4 | 功能缺口 | 🔵 P2 | dashboard 增加按周/月的浏览↔下载转化趋势图、标签云、导出 CSV |
| 跨设备同步 | 见功能缺口 G5 | 功能缺口 | 🔵 P2 | 基于现有备份 JSON，支持导出到云盘/导入实现多机同步；可选端到端加密 |
| 国际化 | 见 §2-H | 功能缺口 | 🔵 P2 | 抽离 UI 文案为 i18n 资源，提供英文界面 |

### 功能缺口明细（本任务书新增识别，供阶段二/三落地）

- **G1 推荐信号缺失**：当前推荐引擎（v3.2.8）仅用「行为信号 + 标签匹配 + 好评率(70%) + 中文支持(30%)」。建议补充 **价格、游戏时长、近期热度** 信号，权重可配置。来源：`background/recommend/engine.js` 的 `computeGameScore`。
- **G2 推荐可解释性缺失**：列表页 🎯 徽章只给概率，不给「为何推荐」。建议在 UI 直观呈现推荐理由（命中标签、行为相似度、评分贡献）。
- **G3 LLM 评分不可解释**：接入 Ollama/OpenAI 时仅给分数，缺可解释输出。建议 LLM 返回结构化理由文本，与 G2 共用展示层。
- **G4 数据分析维度单一**：dashboard 仅有基础统计，缺趋势分析与可导出的运营数据。
- **G5 跨设备数据孤立**：现有备份为本地 JSON，无多机同步路径。

---

## 4. 第三部分：工作方式建议（agent 执行规范）

1. **动手前必读**：每个子任务前 `Read` 相关源文件 + 报告对应章节，确保理解现状；不要凭记忆改代码。
2. **小步快跑、分批迁移**：全局耦合（A）与类型（D）是大改，必须分批、按模块推进，每批保持测试全绿、功能无回归。
3. **测试驱动**：改/增逻辑先想清断言，再写/改 `tests/*.mjs`，然后 `node tests/run-tests.js` 验证。新增纯函数优先补单测。
4. **保持安全与依赖方向**：动 `shared/`、`core/` 时同步检查 SSRF 校验、转义、白名单与单向依赖是否被破坏。
5. **提交规范**：每完成一个独立子任务做一次 git 提交，commit message 写清「做了什么 + 根因/来源 + 测试结论」，沿用 README 变更日志风格。
6. **汇报**：完成一批后，用中文简述改动点、测试结果、遗留风险。

---

## 5. 验收标准（Definition of Done）

- **通用**：`node tests/run-tests.js` 全绿；改动有对应测试；未破坏安全水位与单向依赖；README/变更日志同步更新。
- **阶段一（P0）完成判据**：`noisePattern` 与 `escape*` 无重复定义；`content/**` 完成向 ES Module 迁移（或明确的分批计划已落地首批）；接入 eslint + 关键模块 JSDoc 类型；CI 含 lint/类型检查。
- **阶段二（P1）完成判据**：Steam 解析失败可降级不报错；新增 ≥2 个下载站规则且支持规则市场雏形；推荐引擎含 ≥1 项新信号且 UI 呈现推荐理由；列表扫描按需化。
- **阶段三（P2）完成判据**：dashboard 趋势图/标签云/CSV 导出可用；备份支持云导出/导入同步；UI 文案 i18n 化（至少中英双语骨架）。

---

## 6. 建议起步任务（首个提交范围，最小高价值）

按报告 §9 末尾与 §2 结论，**首个工作单元**建议落地以下两项（投入小、回报高、零线上风险）：

1. **消除重复常量（§2-B）**
   - 将 `noisePattern` 抽入 `shared/constants.js`，`title-parser.js` 与 `detail-page.js` 改为 import。
   - `escapeHtml/escapeAttr` 统一由 `shared/escape.js` 导出，`content/core/common.js` 改为引用，删除重复实现。
   - 补测试：断言「全局仅一份定义」「两个调用点行为一致」。
2. **内容脚本模块化首批（§2-A 起步）**
   - 选一个叶子模块（如 `content/core/common.js` 或 `floats.js`）试水 ES Module 化，验证 `__GR__` 去耦合不破坏 popup/列表/详情功能，作为后续分批迁移样板。

完成上述两项并测试全绿后，再推进类型检查（D）与剩余批量迁移。

---

**请 agent 据此任务书，从 §6 起步任务开始，按 §3 优先级逐阶段推进，并严格遵守 §1 红线与 §4 规范。每阶段结束给出中文进展汇报。**
