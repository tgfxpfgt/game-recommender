# 迭代方案研究 — 机制与架构优化（2026-09）

> 研究范围：功能逻辑优化、机制/流程精简、资源开销降低、解耦·工程化·模块化·量化、
> 消除写死（一切可调可重组）。基于 4 份定向扫描（魔法值盘点 / 站点适配架构 /
> 模块耦合结构 / 资源开销热点），全部结论带 file:line 证据。
> 约束不变：零构建原生 JS、不做 i18n/云同步/跨浏览器/规则市场/MAIN world/全量 TS。

---

## 0. 结论摘要

现状是一个**功能已高度收敛、但"接线"仍靠手工同步**的系统：三大注册表（DB_KEYS 26 /
DATA_MODULES 24 / MODULE_FILES 25）数字已经漂移；一处跨层依赖（list→detail）；4 套
escapeHtml、5 份互斥锁、4 套防抖、4 份 Map+TTL 缓存同构实现；~128 处写死魔法值
（其中 51 处值得进 constants、8 处值得进 settings）；写放大实测"60 条批次 ≈ 40-90 次
文件写 / 3-8MB OPFS"；5 分钟 alarm 每小时白唤醒 SW 12 次。

方案分五个批次（每批 ≤1000 行，遵守发布触发线），核心是**四个"单源化"**：

1. **存储注册表 STORAGE_MODULES 单源**——key/file/cap/ttl/debounce/dirty/backup/reset
   一张表派生全部，新存储模块 6 文件 → 1-2 文件；
2. **机制工厂四件套**（withLock / debounce / TtlCache / withRetry）——清零 17 份同构实现；
3. **站点 feature 矩阵进规则 schema**——INLINE_SECTION_SITES、xdgrid host 判断、
   search.js 站点特判全部收编，新站接入 7 触点 → 1 个数据对象；
4. **内容模块 MODULE_MANIFEST**——tracker/integrity/content-sim 三处手工同步变一张表。

配套**量化体系**（GET_RUNTIME_METRICS + 预算门禁）让后续每笔优化可验证。

---

## 1. 量化基线（现状）

| 维度 | 数值 |
|---|---|
| 源码总行数 | ~21,257 行 JS（background 50.4% / content 23.7% / options 10.5%） |
| 最大文件 | dashboard.js 1,054 / detail-page.js 963 / options.js 881 / manager.js 623 |
| 消息 action | 57 个（contract 与 handlers 100% 对齐，但注册靠两处手工同步） |
| 存储模块 | 24 个 DATA_MODULES / 25 个 MODULE_FILES / 26 个 DB_KEYS（已漂移） |
| 新增存储模块成本 | **改 6 个文件**（constants×2、data-store、reset、必要时 contract+handlers） |
| 新增内置下载站成本 | **7 触点 / 6 文件**（用户规则路径已 0 触点） |
| 写死魔法值 | ~128 处：时间 33 / 容量 35 / 阈值 14 / URL 30 / 选择器 7 / 内联配置 6 |
| 同构重复实现 | 互斥锁 ×5、escapeHtml ×4（口径不一）、防抖 ×4、Map+TTL 缓存 ×4、重试散装 ×5 |
| 60 条评分批次的落库成本 | 40-90 次文件写、~3-8MB OPFS 写 + JSON 序列化 CPU |
| SW 常驻唤醒 | grPeriodicFlush 每 5 分钟唤醒一次（每小时 12 次，守护的只是 2s 防抖） |

---

## 2. 方向 A：去写死（常量治理三层分级）

原则：**不追求"全部可调"**——settings 键每加一个都要同步 UI（test-settings-sync 强制），
盲目提升反而增加接线成本。按三层分级：

### A1. 提升为 settings（8 处，用户可感知行为）
| 值 | 位置 | 理由 |
|---|---|---|
| 夜间主题起止小时（19/7） | shared/settings-utils.js:103 | 已有 themeAutoSwitch/uiThemeDay/Night，补 themeNightStart/End 才完整 |
| API 异常判定窗口/失败率 | background/core/api-monitor.js:16-19 | popup 已展示 failRate，做成"敏感度"档位 |
| RATINGS_BATCH_SIZE=60 | content/list/list-batch.js:18 | 直接影响首屏速度 vs 请求数，与 cacheTtls 同级 |
| 二维码图片上限 3MB | background/handlers.js:235 | 低配设备可调小 |

### A2. 提升为 constants 单源（51 处，代码可调但无需 UI）
- **URL/ENDPOINTS 表**（收益最高）：Steam 商店页前缀 `store.steampowered.com/app/` 重复 **7 处**
  （detail-templates:240,292 / badges:46 / popup:439 / dashboard:870,911 / cache.js:225）；
  storesearch 3 处、appdetails 2 处、ITAD prices 2 处、CDN 回退域 2 处
  （api-details:78 与 detail-page:947）→ 一个 `constants.js ENDPOINTS` 表。
- **存储容量 CAPS 组**：favorites 500、app-stats 20000、history 200、registry 10000、
  nameIndex/urlIndex 5000、searchCache 200、llmCache 300 等 14 处，各散落在自己文件顶部 →
  `CAPS = { favorites: 500, ... }` 集中声明（文件内引用同一常量）。
- **cacheTtls 扩容**：steam250 24h、search 24h、llm 7d、ITAD 12h、steamType 12h、
  api-reviews 冷却 5min/1h —— 现有 `cacheTtls` 设置组机制已可承载，逐个迁入。
- **评分语义刻度**：engine.js:102-119 SteamSpy 四信号归一化分母（600/log10÷5/÷7）、
  中性缺省 0.3/0.4 —— 权重已可调但刻度写死，属"隐藏的评分逻辑"，进 constants 并注释口径。
- **批次族**：内容 60 ↔ 后台 2/6/4 无联动校验 → constants 内相邻声明 + integrity 测试断言
  `RATINGS_BATCH_SIZE % BACKEND_BATCH_SIZE === 0` 类关系。
- **删除假单源**：好评率分级色 80/60 阈值与色值在 badges.js:200-212、detail-templates:148、
  cache.js:225 保留了**整份字面量 fallback**——patterns.js 与 content 同注入必在，删 fallback
  （零成本消除漂移源）。

### A3. 留原地（其余 ~70 处）
UI 提示时长、一次性 fetch 超时、观察器 childList/subtree 等实现细节，提升反而制造噪音。
判据：**改它的人会不会需要同时知道第二个文件？** 不会 → 留原地。

---

## 3. 方向 B：解耦与重组（四张单源表）

### B1. 存储注册表 STORAGE_MODULES（解决 6 文件接线 + 三表漂移）
constants.js 的 DB_KEYS(26)/DATA_MODULES(24) 与 data-store.js 的 MODULE_FILES(25) 是
**同一事实的三份拷贝**且数字已漂移。改为一张表派生全部：

```js
// background/core/storage-registry.js（新，~80 行）
export const STORAGE_MODULES = {
  [DB_KEYS.FAVORITES]: { file: 'favorites.json', format: 'json',
    cap: CAPS.favorites, debounce: false, backup: true, reset: 'resetFavorites' },
  [DB_KEYS.RUNTIME_LOG]: { file: 'runtime-log.ndjson', format: 'ndjson',
    cap: 300, debounce: 2000, backup: false, reset: 'resetRuntimeLog' },
  ...
};
```

- data-store.js 的 readModule/writeModule/appendModule 改为查表（file/format/cap）；
- reset.js、backups.js 核心子集、GET_DATA_MODULES、clearData 全部改为**遍历注册表**，
  reset 例外项（steam250 走业务层）用 `resetVia` 字段表达；
- 新存储模块成本：**1 个业务文件 + 注册表 1 行**（6 文件 → 2 文件）；
- integrity 测试改为断言"注册表 ↔ 磁盘文件 ↔ reset 行为"一致。
- 迁移期保持旧 DB_KEYS/DATA_MODULES 作为派生视图导出（引用不断）。

### B2. 机制工厂四件套（清零 17 份同构实现）
```
background/core/mechanisms.js：
  withLock()      ← 收编 history:37 / logger:47 / behavior:100 / app-stats:79 / download-urls:19（5 份）
  debounce()      ← 收编 session-persist:12 / list-page:94 / options:30 / debug:24（4 份）
  TtlCache()      ← 收编 itadLowestCache / steamTypeCache / searchCache / steam250 rankMap（4 份）
  withRetry()     ← 收编 ratings-batch:239 / api-registry-heal:8 / api-details:86 / download-sites:79（散装 4+ 处）
```
全部纯函数工厂、可注入时钟单测。每个收编点是无行为变化的等价替换 + 既有测试护航。

### B3. 纯逻辑下沉 shared（消除跨层依赖）
- `content/detail/detail-templates.js`（489 行）中 **82% 是 HTML 模板**，但
  adjustedReputation/verdictFor/ratingTextInfo/heatLabelFor（86 行纯逻辑）寄居其中，
  导致 content/list/badges.js:13 **跨子目录 import detail/**（list→detail 反向耦合）。
  → 新建 `content/core/steam-rating-logic.js`（或 shared/），模板与徽章同源引用；
  顺带这 4 个函数获得独立单测（test-ui-pure 已具备设施）。
- `escapeHtml` 4 套实现**口径不一**（rules.js:149,169 的 `esc` 不转义引号——安全口径差异）：
  common.js/options.js/rules.js 统一改用 shared/escape.js，common.js 仅留加载顺序兜底。

### B4. handlers 表驱动 + 业务归位
handlers.js 407 行、57 个 action 中大量纯转发（handleGetSettings:167 等）与内联业务
混杂（handleTrackEvent:75-125 约 50 行 / handleFetchImageDataUrl:226-251）。方向：
- 纯转发类改为表驱动：`{ GET_SETTINGS: [settingsApi, 'getSettings'] }` 一行一个；
- 内联业务继续按既有 handlers/ 子目录模式迁出（track-event.js、image-fetch.js）；
- contract 规则与 handler 绑定同表声明（新 action 从"contract+handlers 两处"降为 1 处），
  integrity 测试校验"contract 键集 === handler 键集"（已有机制）。

### B5. 内容模块 MODULE_MANIFEST
新内容模块现在要同步 tracker ensureModules + test-content-sim MODULE_FILES/MODULE_KEYS
+（可选）manifest。改为一张清单：

```js
// content/module-manifest.js
export const MODULE_MANIFEST = [
  { key: 'qrUnlock', file: 'content/detail/qr-unlock.js', layer: 'detail', optionalSetting: 'qrUnlockEnabled' },
  { key: 'xdgrid', file: 'content/list/xdgrid.js', layer: 'list', optionalSetting: 'xdgridEnabled' },
  ...
];
```
tracker 按清单装载（optional 键映射自动生成），integrity/content-sim 按清单校验。
**加一个内容模块从 3 处 → 1 处**，且 T3 条件加载自动获得清单级测试。

### B6. 站点 feature 矩阵进规则 schema
站点适配已 90% schema 化（adapter=纯数据、builder=唯一解释器、用户规则 0 触点接新站）。
剩余 10% 的硬编码恰好都是 feature×site 关系：
- `detail-page.js:604` INLINE_SECTION_SITES = ['xianyudanji','gamer520']（内嵌卡站点）
- `xdgrid.js:157,423` host.includes('xdgame.com')（布局定制默认启用）
- `search.js:127` siteKey === 'xdgame' 分支、:239 站内搜索链接正则

→ 规则 schema（rules.js SITE_FIELD_TYPES）增加 `features` 字段
（`{ inlineSteamCard: true, gridLayout: 'default', detailMeta: {...} }`），内置 adapter
数据文件同样声明，3 处业务分支改为读规则。**新站接入从 7 触点 → 1 个数据对象**，
用户规则路径获得同等 feature 能力。

### B7. 域名单源
同一域名 5 处独立声明（manifest ×3 套列表 / BUILTIN_DOMAINS / adapter domains /
trackedSites / history 静态映射），其中 web_accessible_resources.matches **不被任何
一致性测试覆盖**（漏改 = 动态模块在该站加载失败）；trackedSites 已含 3 个孤儿域名
（yystv.cn / fitgirl-repacks.site / rutracker.org，无注入声明，死条目）。
→ 方向：scripts/generate-manifest-domains.mjs（零构建兼容的**生成器**而非构建步骤，
  手动/CI 生成 manifest 域名段）或运行时单源 + integrity 扩展覆盖 WAR 段；
  清理 trackedSites 孤儿；删除 history.js inferSiteFromDomain（规则系统已覆盖）。

---

## 4. 方向 C：资源开销（写放大 / SW 唤醒 / 网络 / 观察器）

按"收益/风险"排序（预计把每批落库从 40-90 次写 / 3-8MB 降到 **≤10 次 / <0.5MB**）：

| # | 优化 | 证据 | 动作 | 风险 |
|---|---|---|---|---|
| 1 | runtimeLog 假 NDJSON | logger.js:87 全量重写 + flush 前 readModule 回读（300 条） | 改 appendModule 真追加；截断时才整写；去回读 | 低 |
| 2 | 3 模块 flush 无 dirty 检查 | url-index:48 / wrong-reports:76 / learned-noise:38 | 补 dirty 标记（对齐 app-stats:65 范式；B1 注册表后由 `dirty` 字段统一） | 低 |
| 3 | grPeriodicFlush 5min alarm | service-worker.js:181 | 拉长至 30-60min，或"有脏数据时"动态 one-shot alarm | 低 |
| 4 | SW 冷启动 14 文件全量预热 | service-worker.js:134-149 | 分级：首消息只读 steam-cache+name-index，其余惰性（各模块已有 lazy 兜底） | 中 |
| 5 | GetNewsForApp 每游戏 1 请求 | orchestrator.js:436-438；60 批最多 60 请求仅为一行"最近更新"（列表徽章不消费） | 列表评分路径跳过，详情页按需补 | 低 |
| 6 | 缓存命中触发 registry 全量写 | orchestrator.js:193-209 ensureRegistryEntry | 仅条目缺失/字段变化时置 dirty | 低 |
| 7 | QR Observer 全文档常驻 | qr-unlock.js:213 observe(documentElement, subtree) 全站默认开 | 收窄到详情容器 / 仅网盘域名页开启 | 低 |
| 8 | batchState 无上限 + Observer 常驻 | list-batch.js:62-72 processItems 无限追加；finishRatings 后 observer 不断开 | 按 maxScanLinks 截断 + finish 后 disconnect | 中 |
| 9 | behaviorLog 读放大 | behavior.js:44 每事件 append 后全量读回 | 内存缓存合并 + appendModule 返回值 | 低 |
| 10 | steam-cache 脏模块仍全量序列化 | steam-cache.js:306-335 | 评估 per-appId 分片或 diff 写（B1 落地后做，收益千条级才明显） | 中 |

配套小项：searchCache 写穿（search-cache.js:70）、downloadUrls 即时全量写（:105 与批量版 :135 重复能力）、
每条 Steam API 调用 3 次 session scheduleSave（合并为 1）。

---

## 5. 方向 D：量化体系（让优化可验证、可回归）

现状只有 api-monitor（会话请求数）与 runtimeLog。补一个轻量**运行指标框架**：

```
background/core/metrics.js（~60 行）：
  metricInc(name) / metricObserve(name, ms) —— 内存计数 + 直方图，
  session-persist 落盘（复用现有机制），GET_RUNTIME_METRICS action 透出。
默认采集（每项 <1KB）：
  batch.renderMs（每批渲染耗时）、batch.size、opfs.writeBytes / writeCount（按模块）、
  msg.count（按 action）、fetch.count（按 host）、sw.wakeups、observer.callbacks
展示：dashboard 新增"运行指标"卡（复用 N1 会话计数样式）。
预算门禁（可选第二批）：E2E 冒烟断言 renderMs P95 < 预算、每批写次数 < 预算——
把第 4 节的收益变成 CI 可回归的数字。
```

另：content-sim 已具备把渲染路径跑起来的能力，可加"每 item 消息数 ≤0.15"类断言。

---

## 6. 目标架构蓝图（重组后的排列）

```
shared/          escape(唯一) msg patterns page-kit(主题/防抖壳) crypto-utils scoring?
background/core/ constants(+ENDPOINTS/CAPS) storage-registry(单源表) mechanisms(四件套)
                 contract+handlerTable api-monitor metrics session-persist
background/storage/ 每模块 = 业务薄层 + 注册表一行（lock/debounce/dirty/cap 全部来自机制与注册表）
content/         module-manifest(装载单源) core/(floats status-bar rating-logic) list/ detail/ tracking/
adapters/sites/  纯数据 + features 字段（feature 矩阵唯一表达处）
handlers         表驱动转发 + 业务全在 handlers/*.js 与业务层
```

不变量：MV3 SW 静态 import、内容脚本动态 import、零构建、依赖分层单向（integrity 强制）。

---

## 7. 分批路线图

| 批次 | 内容 | 预估规模 | 风险 | 核心收益 |
|---|---|---|---|---|
| **1 机制与单源收敛** | B2 四件套工厂 + B3 评分口径下沉 + escape 统一 + A2 删分级色 fallback + ENDPOINTS/CAPS 表 | ~600-800 行 | 低（等价替换+测试护航） | 17 份重复实现清零；list→detail 耦合解除；URL 7 处重复消除 |
| **2 存储注册表** | B1 STORAGE_MODULES 单源 + dirty 补齐(#2) + runtimeLog 追加化(#1) + flush dirty 快照 | ~800-1000 行 | 中（迁移期兼容） | 新模块 6→2 文件；三表漂移根治；每批写 -60% |
| **3 开销治理** | #3 alarm 拉长 + #4 分级预热 + #5 GetNewsForApp 跳过 + #6 命中不写 + #7 QR 收窄 + #8 batchState 上限 + 小项 | ~600-800 行 | 低-中 | SW 唤醒 -80%；每批网络 -30%；每批落库 ≤10 次 |
| **4 站点与模块清单** | B6 features 字段 + 3 处收编 + B7 域名单源/孤儿清理 + B5 MODULE_MANIFEST | ~500-700 行 | 中 | 新站 7→1 触点；新内容模块 3→1 处；WAR 漂移风险关闭 |
| **5 handlers+页面+量化** | B4 表驱动 handlers + 业务迁出 + 页面主题/消息/防抖统一 + D 量化框架 + A1 四个 settings 键 | ~700-900 行 | 中 | handlers 407→<150 行；dashboard 可拆；一切可量化 |

每批独立可发布（遵守 ≤1000 行触发线独立走 gate）；批次 1 建议先行——纯等价替换，
是后续所有批次的地基。settings 新增键（A1）须按铁律 #10 同步 options 层与 popup 必需集。

---

## 8. 本方案不做的事（防止过度工程）

- 不把 adapter 文件转纯 JSON（内容脚本经典脚本注入限制下 IIFE 是合理折中，已评估收益最小）；
- 不引入打包器/DI 框架/观察者总线——"机制工厂 + 注册表"已覆盖全部诉求且零构建兼容；
- 不做全量 settings 化（~70 处留原地，判据见 A3）；
- 不重排目录大搬家（批次 5 的 handlers 业务迁出即最大动作，其余均为表内重组）。
